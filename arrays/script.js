var name=["john", "hari", "rumi"]
// name.splice(1,2) // remove 2 elements from position 1 and onwards
// name.push("rintu") //add an eleent to the back
// name.pop() //removes last element
// name.shift() // removes the first element 
// console.log(name.ind  exOf("hari"))  // returns the index of element "hari"
const a="rumi"
if (!name.indexOf(a)===1){
    console.log(`no ${a}`)
}else{
    console.log(` ${a} is here`)
}

console.log(name)
